<h1>EAT-DA-BURGER!!!</h1>
      
<h2>Burgers Yet to DEVOUR:</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block toEat= true}}
    {{/unless}}
  {{/each}}
</ul>
<BR>
<h2>Burgers You Have DEVOURED:</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block toEat= false}}
    {{/if}}
  {{/each}}
</ul>
<BR>
<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burgerName" name="name">
  </div>


  <button type="submit">Add Burger</button>
</form>

